import { toOverviewModel } from '../../../../../src/ui/graphs/overview/d3-overview-graph';

describe('d3-overview-graph', () => {
  it('toOverviewModel creates model', () => {
    const model = toOverviewModel();
  });
});
